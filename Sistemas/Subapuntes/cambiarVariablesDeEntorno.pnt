1. Localizar
entrar en el directorio etc en el fichero environment
2. Modificar
añadir la informacion deseada con ':'
PATH=$PATH(para cojer el actual y asi añadir):(para diferenciar el directorio)/home/{usuario}/scripts

== $PATH =
(juntado con echo) devuelve el archivo /etc/environment